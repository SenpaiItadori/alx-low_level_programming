#include "main.h"

/**
 * main - prints its name followed by a new line
 * @argc: the nemuber of command line arguments
 * @argv: an array containing all command line arguments
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
