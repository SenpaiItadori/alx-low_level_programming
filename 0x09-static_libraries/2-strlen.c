#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns length of string
 * @str: the string to be measured
 * Return: 0
 */

int _strlen(char *str)
{
	return (strlen(str));
}
