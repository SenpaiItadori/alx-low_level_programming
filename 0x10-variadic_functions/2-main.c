#include "variadic_functions.h"

/**
 * main - code checker
 * Return: 0
 */

int main(void)
{

	print_strings(", ", 2, "Jay", "Django");
	print_strings(" :yess:", 2, "Hey, would you like to", " Sure!");
	print_strings("", 4, "th", "e market", " ha", "s crashed", "not");
	return (0);
}
