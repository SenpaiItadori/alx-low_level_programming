#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - compares 2 strings
 * @s1: one of the strings
 * @s2: the other one of em strings
 * Return: the interger result of the compared strings
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
